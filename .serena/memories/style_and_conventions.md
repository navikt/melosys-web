# Style and Conventions

- EditorConfig: 2-space indentation, LF, trim trailing whitespace, insert final newline.
- Prettier: printWidth 120; formatting enforced via prettier.
- Code style: existing codebase is JS + TS; prefer TypeScript for new code where possible (per README).
- Linting: ESLint configured; run `pnpm eslint`.