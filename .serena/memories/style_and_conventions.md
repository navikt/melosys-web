New code should be written in TypeScript where possible. React components use the NAV Aksel design system with React/Redux hooks. ESLint (config in `eslint.config.js` with @typescript-eslint, react, jsx-a11y, import) and Prettier (printWidth 120) enforce lint/style. GraphQL code is generated from `src/graphql` config; use the generated hooks/types when interacting with GraphQL APIs.