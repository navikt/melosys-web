// Nødvendig for lint-staged kjøring av eslint, ikke utvid.
{
  "extends": "./tsconfig.json",
  "include": ["src/**/*", "vite.config.ts", "vitest.config.ts", "playwright.config.ts", "tests/e2e/**/*"],
  "exclude": ["node_modules"]
}
