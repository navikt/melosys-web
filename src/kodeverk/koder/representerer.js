import { kodeset } from 'melosys-kodeverk';

const { representerer } = kodeset;

export default {
  BRUKER: representerer.BRUKER,
  ARBEIDSGIVER: representerer.ARBEIDSGIVER,
  BEGGE: representerer.BEGGE,
};
