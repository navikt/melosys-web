import * as faktaavklaringActions from './actions';
import * as faktaavklaringOperations from './operations';
import * as faktaavklaringTypes from './types';
import * as faktaavklaringSelectors from './selectors';

import fagsakReducers from './reducers';

export {
  faktaavklaringActions,
  faktaavklaringOperations,
  faktaavklaringTypes,
  faktaavklaringSelectors,
};

export default fagsakReducers;
